
/**
 * 
 * Archvio de envio de peticiones al servidor
 * 
 */
document.querySelector('#btn-submit-log-in').addEventListener('click', (e) => {
    e.preventDefault();
    // Verificar datos en el server
    let form = [document.getElementsByName('inp-name').values,document.getElementsByName('inp-password').values]
    // Enviar array con contenido
    
}, false)
